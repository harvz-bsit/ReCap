/**
 * This file serves as the default entry point (the Home route) for the
 * (drawer) group using Expo Router.
 *
 * It simply re-exports the main DashboardScreen component located one level up.
 */
export { default } from '../dashboardscreen';
